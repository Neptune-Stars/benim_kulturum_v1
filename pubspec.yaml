name: benim_kulturum_v1
description: Kampüs hayatını kolaylaştıran asistanım.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  provider: ^6.1.1
  google_fonts: ^6.1.0
  go_router: ^13.2.0
  url_launcher: ^6.3.2
  image_picker: ^1.1.2
  shared_preferences: ^2.5.3
  path_provider: ^2.1.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true